{
	"validation": {
		"required": "{{field}} is required.",
		"invalid_email": "Invalid email address.",
		"exist": "This {{field}} is already present in our database.",
		"not_exist": "This {{field}} is not present in our database.",
		"length_min": "{{field}} must be at least {{min}} characters",
		"length_max": "{{field}} must be less than {{max}} characters",
		"length_between": "{{field}} must between {{min}} to {{max}} characters",
		"in": "{{field}} accept [{{values}}] only",
		"auth": "Invalid auth credentials",
		"file_type": "Accept [{{values}}] only",
		"file_size": "{{field}} can not larger than {{maxSize}}",
		"array": "{{field}} must be an array",
		"json": "{{field}} must be a JSON object",
        "object": "{{field}} must be a valid object",
		"date": "{{field}} must be a valid date",
		"slug": "{{field}} must be valid slug string",
		"alphanumeric": "{{field}} must be alpha-numeric string"
	}
}
